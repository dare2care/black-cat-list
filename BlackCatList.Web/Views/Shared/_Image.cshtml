@model IImageViewModel

@if (Model.ImageId.HasValue)
{
    <img src="@Url.Action("Index", "Images", new { id = Model.ImageId })" class="responsive" alt="Image for @Model.Name" />
}
else
{
    <img src="~/Content/image-not-found.png" class="responsive" alt="Image not found" />
}